{% extends 'emilyBoutikBundle::layout.html.twig' %}

{% block h1 %}
	Ajouter une cat&eacute;gorie
{% endblock %}

{% block content %}
	<form action="" method="post" {{ form_enctype(form) }} class="form form-category">
        {% if app.session.hasFlash("emily_categories") %}
            <div class="messages">
                {{ app.session.flash("emily_categories") }}
            </div>
        {% endif %}
        {{ form_errors(form) }}
        {{ form_row(form.name) }}
		{{ form_rest(form) }}
        <input type="submit" value="Submit" />
    </form>
{% endblock %}